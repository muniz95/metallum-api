import 'package:test/test.dart';

void main() {
  group('RIP', () {
    group('search', () {
      test('should bring a list with 200 records if no filter is applied', () {
        expect(false, true);
      });
      test('should bring a list of records, given a year', () {
        expect(false, true);
      });
    });
  });
}