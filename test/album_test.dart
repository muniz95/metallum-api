import 'package:test/test.dart';

void main() {
  group('Album', () {
    group('search', () {
      test('should bring at least one album released by Agathocles', () {
        expect(false, true);
      });
      test('should bring at least one album entitled "Nemesis"', () {
        expect(false, true);
      });
      test('should bring at least one album released in 1999', () {
        expect(false, true);
      });
      test('should bring at least one album released by a Polish band', () {
        expect(false, true);
      });
      test('should bring at least one album released under Roadrunner Records label', () {
        expect(false, true);
      });
      test('should bring at least one Thrash metal album', () {
        expect(false, true);
      });
      test('should bring at least one EP release', () {
        expect(false, true);
      });
      test('should bring at least one album available in Vinyl', () {
        expect(false, true);
      });
    });
  });
}