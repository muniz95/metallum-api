import 'package:test/test.dart';

void main() {
  group('Label', () {
    group('search', () {
      test('should bring at least one label, given a name', () {
        expect(false, true);
      });
    });
  });
}